선형 모델 실습
0. 데이터 전처리
0-1. 기초통계분석				>> 수치형/카테고리형 데이터 분류
0-2. 시각화				>> 1. 수치형 데이터 : feature별 히스토그램(분포) (( plt.hist(df['col']) )) + 
					>> 2. 카테고리형 데이터 : feature안의 unique category별 분포 (( plt.boxplot ))
0-3. 변수 상관관계 확인			>> feature별 상관관계 (( cor = df.corr() plt.matshow(cor, fignum=1) ))
+ 상관도는 0.8 넘지 않으면 그닥 없다고 생각함 ( 뭐 기준마다 다름 )
0-4. 이상치 탐지
0-5. 결측치 분석
0-6. 인코딩				>> 카테고리형 데이터를 수치형으로 변환 unique한 카테고리를 새로운 열을 추가해서 수치형으로 만듬 (( pd.get_dummies( df, drop_first = True) )) 	
0-7. 훈련/평가 데이터 분리			>> 1. feature / 정답 데이터 분리 X / y
					>> 2. 훈련데이터(80%) / 평가데이터(20%) 분리 X_train, X_test, y_train, y_test
0-8. 특성 스케일링				>> 두 feature 사이의 값이 너무 차이가 나면 ( 0~1 / 10만~100만 ) 큰 쪽으로 맞추려고 하기 때문에 스케일링을 통해 비슷하게 맞춘다. 단 출력값은 빼고 ( X값만 )
					>> StandardScaler / MinMaxScaler ... 내장함수 확인해봐라


즉
데이터 info() describe()를 통해 
1. 총 데이터 수 2. 칼럼 3. 결측치 4. 데이터 타입 확인
데이터 전처리 ( 결측치 채우기 )
칼럼을 분류 ( 수치, 서수, 카테고리.. ) > 재정의 ( .get_dummies로 새로운 칼럼 생성 )
칼럼별 분석 ( 그래프, 히스토그램 ) > 분포 확인 >> 인사이트/outlier 도출
칼럼별 상관관계 확인
상관관계 적은 칼럼(=독립)끼리 선형모델 적용